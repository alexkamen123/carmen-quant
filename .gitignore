.env
__pycache__/
*.egg-info/
.pytest_cache/
*.pyc
data/
!src/**/data/
.venv/
dist/
.DS_Store

# backtest fixtures (market data snapshots, captured locally)
tests/fixtures/price_snapshots/*.json

# 真实持仓 / 账户余额，绝不入库（仅保留 config/portfolio.example.yaml 模板）
config/portfolio.yaml

# 本地 Claude Code 配置（机器级权限，勿入库）
.claude/
# 一次性调研 brief（保留本地，不入代码历史）
ONBOARDING.md
logs/
