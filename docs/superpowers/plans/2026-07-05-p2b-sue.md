# P2b 盈余惊喜(SUE)因子 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 财报出炉后算 SUE=(实际EPS−预期EPS)/历史surprise标准差，|SUE|超阈落独立事件表 + 30天漂移回填（观测档，默认on），并向 PM 注入 miss→防死扛/beat→防卖飞的双向审慎提示（注入档，默认off）。

**架构：** 纯函数 `compute_sue`/`format_sue_note`（signals 层）→ 事件独立表 `earnings_surprise_alerts` + `backfill_sue_outcomes`（db 层，抄 `fill_long_returns` 30d 分支）→ earnings-check 回看落库（观测线）→ 日报 `strategy_node` 一行 join 注入（注入线）。两条线独立 flag、三重护栏保证 off 时逐字节不变。SUE 是占位因子，≥60条 outcome 经 RankIC 校准前禁当 alpha、禁驱动仓位。

**技术栈：** Python / yfinance `get_earnings_dates` / aiosqlite+sqlite3 / statistics.stdev / pytest（tmp_path + AGENT_DB_PATH，脱网 mock）

---

## ⚠️ 设计澄清（实现前请用户确认 · spec §4.1 vs §5 的张力）

spec §4.1 字面写 `format_sue_note` 取"**已成熟**"事件，但 §2/§5 北极星要求"负向惊喜**少亏/避免大亏**、正向惊喜**防卖飞**"——这两者只有在财报后**尽早**警示才成立；若等事件成熟 30 天才注入就成事后诸葛、丧失避损价值。

**本计划的判定（写死进实现）：**
- **注入路径 `format_sue_note`：走"新鲜事件"** —— 取 `earnings_date ∈ [asof − track_days, asof]`（已发布且仍在漂移窗口内）的最近一条事件，`|SUE| ≥ 阈` 即注入双向审慎。**不违反未来函数**：SUE 只用已公开的过去数据（Reported EPS + 历史 surprise σ），警示"未来可能继续漂移"正是 PEAD 本身，未偷看未来价格。
- **成熟闸只加在 outcome/RankIC 路径**：`return_30d` 天然要 30 天后才算得出；`get_sue_alerts(matured_only=True)` 与 `backfill_sue_outcomes` 的成熟闸守此路径。这才是 §6 asof 闸真正适用的地方（"漂移已实现判断/不进 RankIC"）。

> 若用户认为 §4.1 字面意图是"注入也等成熟"，则改为 format 只取 matured 事件——但那会让功能退化成回顾而非预警，与北极星冲突，故默认取前者。**执行第一个任务前在评审点确认这条。**

---

## 文件结构

| 文件 | 职责 |
|---|---|
| 创建 `src/finance_agent/signals/sue_factor.py` | `compute_sue` 纯函数、flag 读取、`format_sue_note` 注入模板 |
| 改 `src/finance_agent/data/yf_utils.py` | `_ticker_earnings_dates_with_retry` 取数重试 + `_extract_surprises` DataFrame 解析纯函数 |
| 改 `src/finance_agent/db/tracker.py` | `earnings_surprise_alerts` DDL+迁移、`save_sue_alert`/`get_sue_alerts`、`backfill_sue_outcomes` 回填 |
| 改 `src/finance_agent/alerts/earnings_trigger.py` | `record_sue_events` 回看落库（观测线，flag 门控） |
| 改 `src/finance_agent/graph/workflow.py` | `strategy_node` join 追加 `sue_note`（一行） |
| 改 `src/finance_agent/agents/prompts.py` | `PM_BATCH_SYSTEM` 补 SUE 红线 |
| 改 `config/settings.yaml` | 双 flag 分层块（观测 on / 注入 off） |
| 改 `src/finance_agent/main.py` | earnings-check 挂 `record_sue_events`（flag on）+ value-report sweep 挂 `backfill_sue_outcomes` |
| 创建 `tests/test_signals/test_sue_factor.py` | compute_sue + format_sue_note 纯函数测试 |
| 创建 `tests/test_data/test_sue_fetch.py` | `_extract_surprises` 脱网 DataFrame 解析测试 |
| 创建 `tests/test_db/test_sue_alerts.py` | 事件表幂等 + 回填成熟闸/原子两腿/边界测试 |

---

## 任务 1：`compute_sue` 纯函数 + flag 读取

**文件：**
- 创建：`src/finance_agent/signals/sue_factor.py`
- 测试：`tests/test_signals/test_sue_factor.py`

- [ ] **步骤 1：编写失败的测试**

```python
# tests/test_signals/test_sue_factor.py
import math
from finance_agent.signals import sue_factor as sf


def _hist(n, base=0.05):
    # n 条历史 surprise（reported_i − estimate_i），带轻微波动确保 σ>0
    return [base + (i % 3) * 0.01 for i in range(n)]


def test_compute_sue_beat_positive():
    sue = sf.compute_sue(reported_eps=2.01, estimate_eps=1.80, hist_surprises=_hist(8))
    assert sue is not None and sue > 0


def test_compute_sue_miss_negative():
    sue = sf.compute_sue(reported_eps=1.50, estimate_eps=1.80, hist_surprises=_hist(8))
    assert sue is not None and sue < 0


def test_compute_sue_sigma_zero_none():
    assert sf.compute_sue(2.0, 1.8, [0.1] * 8) is None  # 全相同 → σ=0


def test_compute_sue_too_few_quarters_none():
    assert sf.compute_sue(2.0, 1.8, _hist(7)) is None  # <8 季


def test_compute_sue_nan_input_none():
    assert sf.compute_sue(float("nan"), 1.8, _hist(8)) is None
    assert sf.compute_sue(2.0, None, _hist(8)) is None


def test_flags_default_off(monkeypatch, tmp_path):
    # 无 settings.yaml → 两档均默认 off
    monkeypatch.setattr(sf, "_CONFIG_DIR", tmp_path)
    assert sf.sue_factor_enabled() is False
    assert sf.sue_pead_alert_enabled() is False
```

- [ ] **步骤 2：运行测试验证失败**

运行：`uv run pytest tests/test_signals/test_sue_factor.py -v`
预期：FAIL，`ModuleNotFoundError: No module named 'finance_agent.signals.sue_factor'`

- [ ] **步骤 3：编写最少实现代码**

```python
# src/finance_agent/signals/sue_factor.py
"""P2b 盈余惊喜(SUE)因子。

SUE = (实际EPS − 预期EPS) / 历史 surprise 标准差。
观测档 sue_factor：算 SUE + 落 earnings_surprise_alerts 表 + 30天漂移回填（纯观测·默认on）。
注入档 sue_pead_alert：向 PM 注入双向审慎（miss→更保守·beat→防卖飞），改核心建议·默认off。

反过拟合：SUE 是占位因子，≥60条 outcome 经 RankIC 校准前禁当已验证 alpha、禁用于加仓。
未来函数：hist_surprises 只取 earnings_date 之前已披露季度；outcome 回填/RankIC 走成熟闸。
"""
from __future__ import annotations

import statistics
from datetime import date, datetime
from pathlib import Path

_CONFIG_DIR = Path(__file__).parents[3] / "config"

_SUE_FACTOR_DEFAULTS = {"enabled": False}
_SUE_PEAD_DEFAULTS = {
    "enabled": False,
    "sigma_threshold": 1.5,
    "min_quarters": 8,
    "track_days": 30,
}


def _load_block(name: str, defaults: dict) -> dict:
    """读 settings.yaml 的 name 块，缺省回落 defaults（照 fundamental_score.py 模式）。"""
    try:
        import yaml
        p = _CONFIG_DIR / "settings.yaml"
        if p.exists():
            with open(p) as f:
                s = yaml.safe_load(f) or {}
            block = s.get(name, {}) or {}
            return {**defaults, **block}
    except Exception:
        pass
    return dict(defaults)


def sue_factor_enabled() -> bool:
    """观测档：算+落库+回填。缺省 False。"""
    return bool(_load_block("sue_factor", _SUE_FACTOR_DEFAULTS)["enabled"])


def sue_pead_alert_enabled() -> bool:
    """注入档：向 PM 注入双向审慎。缺省 False。"""
    return bool(_load_block("sue_pead_alert", _SUE_PEAD_DEFAULTS)["enabled"])


def _sue_pead_cfg() -> dict:
    return _load_block("sue_pead_alert", _SUE_PEAD_DEFAULTS)


def _is_bad(x) -> bool:
    return x is None or (isinstance(x, float) and x != x)  # None 或 NaN


def compute_sue(reported_eps, estimate_eps, hist_surprises, min_quarters: int = 8):
    """SUE = (reported − estimate) / stdev(历史 surprise)。

    hist_surprises: 财报日**之前**已披露季度的 (reported_i − estimate_i) 序列。
    闭嘴闸：样本 < min_quarters / σ==0 / 任一输入 None/NaN → None（不猜不落假值）。
    """
    if _is_bad(reported_eps) or _is_bad(estimate_eps):
        return None
    clean = [s for s in (hist_surprises or []) if not _is_bad(s)]
    if len(clean) < min_quarters:
        return None
    try:
        sigma = statistics.stdev(clean)
    except statistics.StatisticsError:
        return None
    if sigma == 0:
        return None
    return (reported_eps - estimate_eps) / sigma
```

- [ ] **步骤 4：运行测试验证通过**

运行：`uv run pytest tests/test_signals/test_sue_factor.py -v`
预期：6 项 PASS（compute_sue 5 项 + flags 1 项）

- [ ] **步骤 5：Commit**

```bash
git add src/finance_agent/signals/sue_factor.py tests/test_signals/test_sue_factor.py
git commit -m "feat(P2b): compute_sue 纯函数 + SUE 双 flag 读取（TDD）"
```

---

## 任务 2：`_extract_surprises` DataFrame 解析纯函数

**文件：**
- 修改：`src/finance_agent/data/yf_utils.py`（新增纯函数，放模块末尾）
- 测试：`tests/test_data/test_sue_fetch.py`

- [ ] **步骤 1：编写失败的测试**

```python
# tests/test_data/test_sue_fetch.py
import math
import pandas as pd
from finance_agent.data import yf_utils as yp


def _df(rows):
    # rows: [(date_str, estimate, reported)]，构造 get_earnings_dates 形状
    idx = pd.DatetimeIndex([pd.Timestamp(d, tz="US/Eastern") for d, _, _ in rows],
                           name="Earnings Date")
    return pd.DataFrame(
        {"EPS Estimate": [e for _, e, _ in rows],
         "Reported EPS": [r for _, _, r in rows],
         "Surprise(%)": [0.0 for _ in rows]},
        index=idx,
    )


def test_extract_surprises_sorted_ascending():
    df = _df([("2026-04-30", 1.94, 2.01), ("2025-01-30", 2.34, 2.40)])
    out = yp._extract_surprises(df)
    assert [d.isoformat() for d, _, _ in out] == ["2025-01-30", "2026-04-30"]
    assert out[-1][1] == 2.01 and out[-1][2] == 1.94  # (date, reported, estimate)


def test_extract_surprises_drops_nan_rows():
    df = _df([("2026-07-30", 1.89, float("nan")),  # 未发布行 Reported=NaN
              ("2026-04-30", 1.94, 2.01)])
    out = yp._extract_surprises(df)
    assert len(out) == 1 and out[0][0].isoformat() == "2026-04-30"


def test_extract_surprises_empty_or_none():
    assert yp._extract_surprises(None) == []
    assert yp._extract_surprises(pd.DataFrame()) == []
```

- [ ] **步骤 2：运行测试验证失败**

运行：`uv run pytest tests/test_data/test_sue_fetch.py -v`
预期：FAIL，`AttributeError: module ... has no attribute '_extract_surprises'`

- [ ] **步骤 3：编写最少实现代码**

在 `src/finance_agent/data/yf_utils.py` 末尾追加：

```python
def _extract_surprises(df):
    """把 get_earnings_dates 的 DataFrame 解析成按日期升序的 [(date, reported, estimate)]。

    剔除 Reported EPS / EPS Estimate 为 NaN 的行（未发布或缺数据）。
    列名固定 'EPS Estimate' / 'Reported EPS'（driver: yfinance get_earnings_dates·驼峰空格勿与 earnings_history 混用）。
    """
    if df is None or len(df) == 0:
        return []
    out = []
    for idx, row in df.iterrows():
        est = row.get("EPS Estimate")
        rep = row.get("Reported EPS")
        if est is None or rep is None:
            continue
        if (isinstance(est, float) and est != est) or (isinstance(rep, float) and rep != rep):
            continue
        try:
            d = idx.date()
        except AttributeError:
            d = idx
        out.append((d, float(rep), float(est)))
    out.sort(key=lambda x: x[0])
    return out
```

- [ ] **步骤 4：运行测试验证通过**

运行：`uv run pytest tests/test_data/test_sue_fetch.py -v`
预期：3 项 PASS

- [ ] **步骤 5：Commit**

```bash
git add src/finance_agent/data/yf_utils.py tests/test_data/test_sue_fetch.py
git commit -m "feat(P2b): _extract_surprises 脱网解析 get_earnings_dates（TDD）"
```

---

## 任务 3：`_ticker_earnings_dates_with_retry` 取数重试封装

**文件：**
- 修改：`src/finance_agent/data/yf_utils.py`（照抄 `_ticker_calendar_with_retry:61-71` 结构）

> 说明：此函数直连网络，不做联网单测（与 `_ticker_calendar_with_retry` 一致——项目里取数层不 mock 网络，只对解析层单测）。正确性由任务 8 端到端 monkeypatch 覆盖。

- [ ] **步骤 1：编写实现代码**

在 `src/finance_agent/data/yf_utils.py` 追加（`_ticker_calendar_with_retry` 附近）：

```python
def _ticker_earnings_dates_with_retry(ticker: str, retries: int = 3, limit: int = 20):
    """同步获取 yf.Ticker(ticker).get_earnings_dates(limit=)，失败静默返回 None。

    照抄 _ticker_calendar_with_retry：for-retry + except Exception + sleep(2)（非末次）。
    _YF_SEM 已在模块顶部（Semaphore(2)）；本函数由 executor 包装同步调用，信号量在异步调用侧持有。
    """
    for attempt in range(retries):
        try:
            return yf.Ticker(ticker).get_earnings_dates(limit=limit)
        except Exception:
            if attempt < retries - 1:
                time.sleep(2)
    return None
```

- [ ] **步骤 2：静态自检**

运行：`uv run python -c "from finance_agent.data.yf_utils import _ticker_earnings_dates_with_retry; print('ok')"`
预期：打印 `ok`（导入不报错）

- [ ] **步骤 3：Commit**

```bash
git add src/finance_agent/data/yf_utils.py
git commit -m "feat(P2b): _ticker_earnings_dates_with_retry 取数重试封装"
```

---

## 任务 4：事件表 DDL + 迁移 + `save_sue_alert` / `get_sue_alerts`

**文件：**
- 修改：`src/finance_agent/db/tracker.py`（DDL 进 `_CREATE_SQL`；迁移进新 `_migrate_sue_table` 并在 `init_db` 注册；CRUD 放 `dip_alerts` CRUD 附近）
- 测试：`tests/test_db/test_sue_alerts.py`

- [ ] **步骤 1：编写失败的测试**

```python
# tests/test_db/test_sue_alerts.py
from finance_agent.db import tracker


def test_save_sue_alert_idempotent(tmp_path):
    db = tmp_path / "t.db"
    tracker.init_db(db)
    for _ in range(2):  # 同 (ticker, earnings_date) 写两次
        tracker.save_sue_alert(ticker="AAPL", market="us", earnings_date="2026-04-30",
                               sue_score=-1.8, eps_reported=1.5, eps_estimate=1.8,
                               surprise_std=0.15, db_path=db)
    rows = tracker.get_sue_alerts("AAPL", db_path=db)
    assert len(rows) == 1
    assert rows[0]["sue_score"] == -1.8 and rows[0]["return_30d"] is None


def test_get_sue_alerts_matured_only_excludes_immature(tmp_path):
    # 抄 test_reflection.py::test_asof_excludes_immature 的 asof 成熟闸口径
    db = tmp_path / "t.db"
    tracker.init_db(db)
    # 成熟事件：距 asof 40 天 + 有 outcome
    tracker.save_sue_alert("MU", "us", "2026-05-01", 2.0, 25.1, 20.7, 3.0, db_path=db)
    _set_outcome(db, "MU", "2026-05-01", return_30d=8.0, benchmark_return_30d=2.0)
    # 未成熟事件：距 asof 2 天
    tracker.save_sue_alert("NVDA", "us", "2026-06-08", 1.9, 1.87, 1.77, 0.05, db_path=db)
    matured = tracker.get_sue_alerts(matured_only=True, asof="2026-06-10", db_path=db)
    tickers = {r["ticker"] for r in matured}
    assert tickers == {"MU"}  # NVDA 距 asof<30 天被排除、且无 outcome


def _set_outcome(db, ticker, earnings_date, return_30d, benchmark_return_30d):
    from finance_agent.db.tracker import _conn, _resolve_db
    with _conn(_resolve_db(db)) as con:
        con.execute(
            "UPDATE earnings_surprise_alerts SET price_30d=?, return_30d=?, benchmark_return_30d=? "
            "WHERE ticker=? AND earnings_date=?",
            (100.0, return_30d, benchmark_return_30d, ticker, earnings_date))
```

- [ ] **步骤 2：运行测试验证失败**

运行：`uv run pytest tests/test_db/test_sue_alerts.py -v`
预期：FAIL，`AttributeError: module ... has no attribute 'save_sue_alert'`（或建表缺失）

- [ ] **步骤 3：编写最少实现代码**

3a. 在 `tracker.py` 的 `_CREATE_SQL` 里加表（紧随 `dip_alerts` 表定义）：

```sql
CREATE TABLE IF NOT EXISTS earnings_surprise_alerts (
    id                    INTEGER PRIMARY KEY AUTOINCREMENT,
    ticker                TEXT NOT NULL,
    market                TEXT NOT NULL DEFAULT 'us',
    earnings_date         TEXT NOT NULL,
    sue_score             REAL,
    eps_reported          REAL,
    eps_estimate          REAL,
    surprise_std          REAL,
    price_at_event        REAL,
    price_30d             REAL,
    return_30d            REAL,
    benchmark_return_30d  REAL,
    created_at            TEXT DEFAULT (datetime('now'))
);
CREATE INDEX IF NOT EXISTS idx_sue_ticker ON earnings_surprise_alerts(ticker);
CREATE UNIQUE INDEX IF NOT EXISTS idx_sue_uniq ON earnings_surprise_alerts(ticker, earnings_date);
```

3b. 新增迁移函数（照 `_migrate_dip_alerts_table:110-119`），并在 `init_db` 的迁移调用序列里追加 `_migrate_sue_table(con)`：

```python
def _migrate_sue_table(con: sqlite3.Connection) -> None:
    """为已存在的 earnings_surprise_alerts 补 outcome 列（向后兼容旧库）。
    旧库若无此表，_CREATE_SQL 的 CREATE TABLE IF NOT EXISTS 已建；此处只补列。"""
    try:
        existing = {row[1] for row in con.execute(
            "PRAGMA table_info(earnings_surprise_alerts)").fetchall()}
    except sqlite3.OperationalError:
        return  # 表还没建（老库首次 init 会先跑 executescript），跳过
    for col, typ in (("price_at_event", "REAL"), ("price_30d", "REAL"),
                     ("return_30d", "REAL"), ("benchmark_return_30d", "REAL")):
        if col not in existing:
            try:
                con.execute(f"ALTER TABLE earnings_surprise_alerts ADD COLUMN {col} {typ}")
            except sqlite3.OperationalError:
                pass
```

3c. CRUD（放 `save_dip_alert` 附近）：

```python
def save_sue_alert(ticker, market, earnings_date, sue_score, eps_reported,
                   eps_estimate, surprise_std, price_at_event=None, db_path=None):
    """写 SUE 事件行，(ticker, earnings_date) 幂等（UNIQUE index + INSERT OR IGNORE）。"""
    p = _resolve_db(db_path)
    with _conn(p) as con:
        con.execute(
            "INSERT OR IGNORE INTO earnings_surprise_alerts "
            "(ticker, market, earnings_date, sue_score, eps_reported, eps_estimate, "
            " surprise_std, price_at_event) VALUES (?,?,?,?,?,?,?,?)",
            (ticker.upper(), market, earnings_date, sue_score, eps_reported,
             eps_estimate, surprise_std, price_at_event))


def get_sue_alerts(ticker=None, db_path=None, matured_only=False, asof=None):
    """读 SUE 事件（默认按 earnings_date DESC）。

    ticker=None → 读全部。
    matured_only=True → 仅 return_30d 非空且已过成熟闸（earnings_date 距 asof ≥30 日历日），供 RankIC/outcome。
    """
    p = _resolve_db(db_path)
    where, params = [], []
    if ticker:
        where.append("ticker = ?")
        params.append(ticker.upper())
    if matured_only:
        where.append("return_30d IS NOT NULL")
        where.append("julianday(?) - julianday(earnings_date) >= 30")
        params.append(asof or datetime.today().strftime("%Y-%m-%d"))
    sql = "SELECT * FROM earnings_surprise_alerts"
    if where:
        sql += " WHERE " + " AND ".join(where)
    sql += " ORDER BY earnings_date DESC"
    with _conn(p) as con:
        return [dict(r) for r in con.execute(sql, params).fetchall()]
```

> 依赖确认：`_conn` 已设 `row_factory`（`fill_long_returns` 里用 `row["ticker"]` 证），故 `dict(r)` 可用；`datetime` 已在 tracker 顶部导入。

- [ ] **步骤 4：运行测试验证通过**

运行：`uv run pytest tests/test_db/test_sue_alerts.py -v`
预期：`test_save_sue_alert_idempotent` + `test_get_sue_alerts_matured_only_excludes_immature` PASS

- [ ] **步骤 5：Commit**

```bash
git add src/finance_agent/db/tracker.py tests/test_db/test_sue_alerts.py
git commit -m "feat(P2b): earnings_surprise_alerts 表 + save/get + 幂等/成熟闸（TDD）"
```

---

## 任务 5：`backfill_sue_outcomes` 30天漂移回填

**文件：**
- 修改：`src/finance_agent/db/tracker.py`（抄 `fill_long_returns` 30d 分支）
- 测试：`tests/test_db/test_sue_alerts.py`（追加）

- [ ] **步骤 1：编写失败的测试**

```python
# tests/test_db/test_sue_alerts.py 追加
import asyncio
import finance_agent.db.tracker as tk


def test_backfill_maturity_gate_skips_immature(tmp_path, monkeypatch):
    db = tmp_path / "t.db"
    tk.init_db(db)
    # 事件日在 cutoff/floor 窗口内、但窗口未走满（last_idx<=21）→ 整行跳过留 NULL
    tk.save_sue_alert("AAPL", "us", "2026-06-10", -1.8, 1.5, 1.8, 0.15, db_path=db)

    def fake_paired(t, m, d, fwd_td=21):
        return (100.0, 108.0, "2026-06-10", "2026-06-25", 15)  # last_idx=15<=21 未成熟

    monkeypatch.setattr(tk, "_fetch_paired_window", fake_paired)
    res = asyncio.run(tk.backfill_sue_outcomes(db_path=db))
    assert res["immature"] == 1 and res["filled"] == 0
    assert tk.get_sue_alerts("AAPL", db_path=db)[0]["return_30d"] is None


def test_backfill_atomic_legs_benchmark_fail_leaves_null(tmp_path, monkeypatch):
    db = tmp_path / "t.db"
    tk.init_db(db)
    tk.save_sue_alert("AAPL", "us", "2026-05-01", -1.8, 1.5, 1.8, 0.15, db_path=db)
    monkeypatch.setattr(tk, "_fetch_paired_window",
                        lambda t, m, d, fwd_td=21: (100.0, 110.0, "2026-05-01", "2026-05-30", 25))
    monkeypatch.setattr(tk, "_fetch_benchmark_window", lambda m, s, e: None)  # 基准腿失败
    res = asyncio.run(tk.backfill_sue_outcomes(db_path=db))
    assert res["failed"] == 1 and res["filled"] == 0
    assert tk.get_sue_alerts("AAPL", db_path=db)[0]["return_30d"] is None


def test_backfill_success_writes_both_legs(tmp_path, monkeypatch):
    db = tmp_path / "t.db"
    tk.init_db(db)
    tk.save_sue_alert("AAPL", "us", "2026-05-01", -1.8, 1.5, 1.8, 0.15, db_path=db)
    monkeypatch.setattr(tk, "_fetch_paired_window",
                        lambda t, m, d, fwd_td=21: (100.0, 110.0, "2026-05-01", "2026-05-30", 25))
    monkeypatch.setattr(tk, "_fetch_benchmark_window", lambda m, s, e: 3.5)
    res = asyncio.run(tk.backfill_sue_outcomes(db_path=db))
    row = tk.get_sue_alerts("AAPL", db_path=db)[0]
    assert res["filled"] == 1
    assert row["return_30d"] == 10.0 and row["benchmark_return_30d"] == 3.5
```

> 注：`test_backfill_maturity_gate` 的 `2026-06-10` 相对固定 mock 无关；但 `pending` 的 cutoff/floor 用 `datetime.today()`。为让测试稳定，任务里 `backfill_sue_outcomes` 用 `_today()` 包装（下 3b），测试可 monkeypatch。若不 patch，`2026-06-10`/`2026-05-01` 需落在 [today−30, today−63... ] 窗口内——**实现步骤 3b 增加 `cutoff_days`/`floor` 逻辑后，在测试顶部 monkeypatch `tk._today` 返回固定 `date(2026,7,1)` 保证窗口命中**（见步骤 3c 测试补丁）。

- [ ] **步骤 2：运行测试验证失败**

运行：`uv run pytest tests/test_db/test_sue_alerts.py -k backfill -v`
预期：FAIL，`AttributeError: ... 'backfill_sue_outcomes'`

- [ ] **步骤 3：编写最少实现代码**

3a. 若 tracker 无 `_today` 辅助则新增（便于测试注入）：

```python
def _today():
    return datetime.today()
```

3b. 回填函数（放 `fill_long_returns` 之后）：

```python
async def backfill_sue_outcomes(db_path=None):
    """回填 earnings_surprise_alerts 的 30天漂移 outcome。抄 fill_long_returns 30d 分支：

    - 成熟闸严格：last_idx <= 21（fwd_td）整行跳过，防写盘中/半根价；
    - 个股/基准原子两腿：都成功才写，任一失败该行留 NULL（防 strand）；
    - cutoff/floor 双边界：永久无法回填的行退出 pending（零无谓请求）。
    """
    p = _resolve_db(db_path)
    fwd_td = 21
    cutoff = (_today() - timedelta(days=30)).strftime("%Y-%m-%d")
    floor = (_today() - timedelta(days=fwd_td * 2 + 14 + 7)).strftime("%Y-%m-%d")
    filled = immature = failed = 0
    loop = asyncio.get_event_loop()
    with _conn(p) as con:
        pending = con.execute(
            "SELECT id, ticker, earnings_date, COALESCE(market,'us') AS market "
            "FROM earnings_surprise_alerts "
            "WHERE earnings_date <= ? AND earnings_date >= ? AND return_30d IS NULL",
            (cutoff, floor),
        ).fetchall()
    for row in pending:
        win = await loop.run_in_executor(
            None, lambda t=row["ticker"], m=row["market"], d=row["earnings_date"]:
            _fetch_paired_window(t, m, d, fwd_td=fwd_td))
        if win is None:
            failed += 1
            continue
        p0, p_exit, start_date, exit_date, last_idx = win
        if last_idx <= fwd_td:
            immature += 1
            continue
        bm = await loop.run_in_executor(
            None, lambda m=row["market"], s=start_date, e=exit_date:
            _fetch_benchmark_window(m, s, e))
        if bm is None:
            failed += 1
            continue
        with _conn(p) as con:
            con.execute(
                "UPDATE earnings_surprise_alerts SET price_30d=?, return_30d=?, "
                "benchmark_return_30d=? WHERE id=?",
                (round(p_exit, 4), round((p_exit - p0) / p0 * 100, 2),
                 round(bm, 2), row["id"]))
        filled += 1
    return {"filled": filled, "immature": immature, "failed": failed}
```

3c. 在 3 个 backfill 测试顶部加窗口稳定补丁（每个测试内 `monkeypatch.setattr`）：

```python
    monkeypatch.setattr(tk, "_today", lambda: __import__("datetime").datetime(2026, 7, 1))
```

（把此行加在每个 backfill 测试 `init_db` 之后、`save_sue_alert` 之前。2026-06-10 距 2026-07-01 为 21 天 ≤30 → 在 cutoff 内；2026-05-01 距 61 天 → 仍 ≥ floor(2026-07-01−63=2026-04-29)。均命中 pending。）

- [ ] **步骤 4：运行测试验证通过**

运行：`uv run pytest tests/test_db/test_sue_alerts.py -v`
预期：全部 PASS（幂等 + 成熟闸 + 原子两腿 + 成功两腿 + 成熟排除）

- [ ] **步骤 5：Commit**

```bash
git add src/finance_agent/db/tracker.py tests/test_db/test_sue_alerts.py
git commit -m "feat(P2b): backfill_sue_outcomes 30天漂移回填（成熟闸+原子两腿·TDD）"
```

---

## 任务 6：`format_sue_note` 双向审慎注入模板

**文件：**
- 修改：`src/finance_agent/signals/sue_factor.py`（新增 `format_sue_note` + `_to_date`）
- 测试：`tests/test_signals/test_sue_factor.py`（追加）

- [ ] **步骤 1：编写失败的测试**

```python
# tests/test_signals/test_sue_factor.py 追加
from finance_agent.db import tracker


def _enable_inject(monkeypatch):
    monkeypatch.setattr(sf, "_sue_pead_cfg",
                        lambda: {"enabled": True, "sigma_threshold": 1.5,
                                 "min_quarters": 8, "track_days": 30})


def test_format_miss_wording(tmp_path, monkeypatch):
    _enable_inject(monkeypatch)
    db = tmp_path / "t.db"; tracker.init_db(db)
    tracker.save_sue_alert("AAPL", "us", "2026-06-25", -2.0, 1.5, 1.8, 0.15, db_path=db)
    note = sf.format_sue_note("AAPL", asof="2026-06-27", db_path=db)
    assert "更保守" in note or "止损复核" in note
    assert "加仓" not in note and "买入" not in note and "更激进" not in note


def test_format_beat_wording(tmp_path, monkeypatch):
    _enable_inject(monkeypatch)
    db = tmp_path / "t.db"; tracker.init_db(db)
    tracker.save_sue_alert("NVDA", "us", "2026-06-25", 2.3, 1.87, 1.77, 0.04, db_path=db)
    note = sf.format_sue_note("NVDA", asof="2026-06-27", db_path=db)
    assert "额外确认" in note or "别慌卖" in note
    assert "加仓" not in note and "买入" not in note and "更激进" not in note


def test_format_below_threshold_empty(tmp_path, monkeypatch):
    _enable_inject(monkeypatch)
    db = tmp_path / "t.db"; tracker.init_db(db)
    tracker.save_sue_alert("AAPL", "us", "2026-06-25", 0.8, 1.9, 1.8, 0.12, db_path=db)
    assert sf.format_sue_note("AAPL", asof="2026-06-27", db_path=db) == ""


def test_format_off_returns_empty_bytewise(tmp_path, monkeypatch):
    # 注入档 off → 恒 ""（三重护栏第一层）
    monkeypatch.setattr(sf, "_sue_pead_cfg",
                        lambda: {"enabled": False, "sigma_threshold": 1.5,
                                 "min_quarters": 8, "track_days": 30})
    db = tmp_path / "t.db"; tracker.init_db(db)
    tracker.save_sue_alert("AAPL", "us", "2026-06-25", -2.0, 1.5, 1.8, 0.15, db_path=db)
    assert sf.format_sue_note("AAPL", asof="2026-06-27", db_path=db) == ""


def test_format_stale_event_out_of_window_empty(tmp_path, monkeypatch):
    # 事件距 asof > track_days(30) → 漂移窗口外，不注入
    _enable_inject(monkeypatch)
    db = tmp_path / "t.db"; tracker.init_db(db)
    tracker.save_sue_alert("AAPL", "us", "2026-05-01", -2.0, 1.5, 1.8, 0.15, db_path=db)
    assert sf.format_sue_note("AAPL", asof="2026-06-27", db_path=db) == ""


def test_format_future_event_excluded(tmp_path, monkeypatch):
    # earnings_date > asof（未来）→ 未来函数防护，不注入
    _enable_inject(monkeypatch)
    db = tmp_path / "t.db"; tracker.init_db(db)
    tracker.save_sue_alert("AAPL", "us", "2026-07-30", -2.0, 1.5, 1.8, 0.15, db_path=db)
    assert sf.format_sue_note("AAPL", asof="2026-06-27", db_path=db) == ""
```

- [ ] **步骤 2：运行测试验证失败**

运行：`uv run pytest tests/test_signals/test_sue_factor.py -k format -v`
预期：FAIL，`AttributeError: ... 'format_sue_note'`

- [ ] **步骤 3：编写最少实现代码**

在 `sue_factor.py` 追加：

```python
def _to_date(v):
    if v is None:
        return None
    if isinstance(v, date) and not isinstance(v, datetime):
        return v
    if isinstance(v, datetime):
        return v.date()
    try:
        return datetime.strptime(str(v)[:10], "%Y-%m-%d").date()
    except ValueError:
        return None


def format_sue_note(ticker, asof=None, db_path=None) -> str:
    """【盈余惊喜】双向审慎注入（strategy_evidence 槽）。

    注入档 off → ""（三重护栏第一层）。确定性模板、零 LLM/网络。
    取该 ticker 最近一条『已发布且仍在漂移窗口内(earnings_date ∈ [asof−track_days, asof])』的事件，
    |SUE| ≥ sigma_threshold 才出文字。措辞只单向收紧：
      miss(SUE≤−阈) → 更保守/止损复核；beat(SUE≥+阈) → 减仓/止损前多确认防卖飞。绝无加仓字样。
    """
    from finance_agent.db.tracker import get_sue_alerts  # 延迟导入避免循环依赖
    cfg = _sue_pead_cfg()
    if not cfg["enabled"]:
        return ""
    asof_d = _to_date(asof) or datetime.today().date()
    track = int(cfg.get("track_days", 30))
    thr = float(cfg.get("sigma_threshold", 1.5))
    try:
        rows = get_sue_alerts(ticker, db_path=db_path)  # 按 earnings_date DESC
    except Exception:
        return ""
    for r in rows:
        ed = _to_date(r.get("earnings_date"))
        if ed is None or ed > asof_d:
            continue                       # 未来事件不注入（未来函数防护）
        if (asof_d - ed).days > track:
            continue                       # 超出漂移窗口不再警示
        sue = r.get("sue_score")
        if sue is None or abs(sue) < thr:
            return ""                       # 最近一条也不过阈 → 不注入
        if sue <= -thr:
            return (f"【盈余惊喜】{ticker} 最新财报大幅低于预期(SUE={sue:+.1f}σ)，"
                    f"警惕盈余后下行漂移，宜更保守/止损复核。")
        return (f"【盈余惊喜】{ticker} 最新财报录强正向盈余惊喜(SUE={sue:+.1f}σ)，"
                f"短期回调或为噪声，减仓/止损前需额外确认，别慌卖。")
    return ""
```

- [ ] **步骤 4：运行测试验证通过**

运行：`uv run pytest tests/test_signals/test_sue_factor.py -v`
预期：全部 PASS（compute 5 + flags 1 + format 6）

- [ ] **步骤 5：Commit**

```bash
git add src/finance_agent/signals/sue_factor.py tests/test_signals/test_sue_factor.py
git commit -m "feat(P2b): format_sue_note 双向审慎注入·单向收紧·三重护栏第一层（TDD）"
```

---

## 任务 7：earnings-check 回看落库（观测线）

**文件：**
- 修改：`src/finance_agent/alerts/earnings_trigger.py`（新增 `record_sue_events`，照 `check_and_alert_earnings` 遍历骨架）
- 测试：`tests/test_db/test_sue_alerts.py`（追加端到端 monkeypatch 测试）

- [ ] **步骤 1：编写失败的测试**

```python
# tests/test_db/test_sue_alerts.py 追加
import datetime as _dt
import pandas as pd
import finance_agent.alerts.earnings_trigger as et


def test_record_sue_events_writes_fresh_earnings(tmp_path, monkeypatch):
    db = tmp_path / "t.db"; tk.init_db(db)
    # 构造 12 季 earnings（最近一条落在近几日 → 刚出炉），mock 取数
    dates = pd.date_range("2023-06-01", periods=12, freq="90D", tz="US/Eastern")
    dates = dates.append(pd.DatetimeIndex([pd.Timestamp("2026-06-30", tz="US/Eastern")]))
    df = pd.DataFrame({
        "EPS Estimate": [1.0 + 0.02 * i for i in range(13)],
        "Reported EPS": [1.0 + 0.02 * i + 0.30 for i in range(13)],  # 稳定 beat，末季大 beat
        "Surprise(%)": [0.0] * 13,
    }, index=pd.DatetimeIndex(list(dates), name="Earnings Date"))

    monkeypatch.setattr(et, "_load_us_holdings", lambda: [{"ticker": "AAPL", "market": "us"}])
    monkeypatch.setattr(et, "_ticker_earnings_dates_with_retry", lambda t, limit=20: df)
    monkeypatch.setattr(et, "_today_date", lambda: _dt.date(2026, 7, 1))
    import asyncio
    asyncio.run(et.record_sue_events(db_path=db))
    rows = tk.get_sue_alerts("AAPL", db_path=db)
    assert len(rows) == 1 and rows[0]["earnings_date"] == "2026-06-30"
    assert rows[0]["sue_score"] is not None


def test_record_sue_events_skips_stale_earnings(tmp_path, monkeypatch):
    db = tmp_path / "t.db"; tk.init_db(db)
    dates = pd.date_range("2023-06-01", periods=13, freq="90D", tz="US/Eastern")  # 最近一条也是几个月前
    df = pd.DataFrame({
        "EPS Estimate": [1.0 + 0.02 * i for i in range(13)],
        "Reported EPS": [1.0 + 0.02 * i + 0.30 for i in range(13)],
        "Surprise(%)": [0.0] * 13,
    }, index=pd.DatetimeIndex(list(dates), name="Earnings Date"))
    monkeypatch.setattr(et, "_load_us_holdings", lambda: [{"ticker": "AAPL", "market": "us"}])
    monkeypatch.setattr(et, "_ticker_earnings_dates_with_retry", lambda t, limit=20: df)
    monkeypatch.setattr(et, "_today_date", lambda: _dt.date(2026, 7, 1))
    import asyncio
    asyncio.run(et.record_sue_events(db_path=db))
    assert tk.get_sue_alerts("AAPL", db_path=db) == []  # 最近财报非近3交易日 → 不落
```

- [ ] **步骤 2：运行测试验证失败**

运行：`uv run pytest tests/test_db/test_sue_alerts.py -k record_sue -v`
预期：FAIL，`AttributeError: ... 'record_sue_events'`

- [ ] **步骤 3：编写最少实现代码**

在 `earnings_trigger.py` 顶部补导入 + 常量，并新增函数：

```python
import statistics
import pandas as pd
from finance_agent.data.yf_utils import _ticker_earnings_dates_with_retry, _extract_surprises
from finance_agent.signals.sue_factor import compute_sue, _sue_pead_cfg
from finance_agent.db.tracker import save_sue_alert

SUE_LOOKBACK_TD = 3  # 近 N 个交易日内的财报判为"刚出炉"


def _today_date():
    return datetime.date.today()


def _load_us_holdings():
    config_path = Path(__file__).parents[3] / "config" / "portfolio.yaml"
    with open(config_path) as f:
        portfolio = yaml.safe_load(f)
    return [h for h in portfolio.get("holdings", []) if h.get("market", "us") == "us"]


def _td_since(d, today):
    """d 到 today 的交易日跨度（bdate 近似，含跨周末）。d>today → 0。"""
    if d > today:
        return 0
    return max(0, len(pd.bdate_range(d, today)) - 1)


async def record_sue_events(db_path=None):
    """观测线：对每只美股持仓，若最近一次已发布财报落在近 SUE_LOOKBACK_TD 交易日内 → 算 SUE 落库。

    调用方须在 sue_factor_enabled() 为真时才调；off 时根本不调，earnings-check 逐字节不变。
    单票失败降级（try/except continue），不拖垮整轮。幂等由 save_sue_alert 的 (ticker,earnings_date) 保证。
    """
    cfg = _sue_pead_cfg()
    min_q = int(cfg.get("min_quarters", 8))
    today = _today_date()
    loop = asyncio.get_event_loop()
    recorded = []
    for h in _load_us_holdings():
        ticker = h["ticker"]
        try:
            df = await loop.run_in_executor(
                None, lambda t=ticker: _ticker_earnings_dates_with_retry(t, limit=20))
            surprises = _extract_surprises(df)  # 升序 [(date, reported, estimate)]
            if not surprises:
                continue
            last_date, last_rep, last_est = surprises[-1]
            if _td_since(last_date, today) > SUE_LOOKBACK_TD:
                continue  # 最近财报非"刚出炉"
            hist = [rep - est for (_, rep, est) in surprises[:-1]]  # 当期之前（未来函数防护）
            sue = compute_sue(last_rep, last_est, hist, min_quarters=min_q)
            if sue is None:
                continue
            sigma = statistics.stdev(hist) if len(hist) >= 2 else None
            save_sue_alert(ticker=ticker, market="us", earnings_date=last_date.isoformat(),
                           sue_score=sue, eps_reported=last_rep, eps_estimate=last_est,
                           surprise_std=sigma, db_path=db_path)
            recorded.append((ticker, sue))
        except Exception:
            continue
    return recorded
```

> 确认 `earnings_trigger.py` 顶部已有 `import asyncio`、`import datetime`、`import yaml`、`from pathlib import Path`（`check_and_alert_earnings` 已用）；缺则补。

- [ ] **步骤 4：运行测试验证通过**

运行：`uv run pytest tests/test_db/test_sue_alerts.py -k record_sue -v`
预期：`writes_fresh_earnings` + `skips_stale_earnings` PASS

- [ ] **步骤 5：Commit**

```bash
git add src/finance_agent/alerts/earnings_trigger.py tests/test_db/test_sue_alerts.py
git commit -m "feat(P2b): record_sue_events earnings-check 回看落库·观测线（TDD）"
```

---

## 任务 8：`strategy_node` 注入 + PM 红线

**文件：**
- 修改：`src/finance_agent/graph/workflow.py:281`（join 追加 `sue`）
- 修改：`src/finance_agent/agents/prompts.py:129-132`（`PM_BATCH_SYSTEM` 补红线）

- [ ] **步骤 1：改 `strategy_node`（workflow.py，`senti` 赋值之后、`combined=` 之前）**

```python
        sue = ""
        try:
            from finance_agent.signals.sue_factor import format_sue_note
            sue = format_sue_note(s.ticker)
        except Exception:
            sue = ""
        combined = "\n".join(x for x in (evidence, live, refl, dip_note, senti, sue) if x)
```

（把原 `combined = "\n".join(x for x in (evidence, live, refl, dip_note, senti) if x)` 替换为上面两段——新增 `sue` 块 + join 序列尾加 `sue`。空串被 `if x` 天然过滤 → 注入档 off 时 `format_sue_note` 恒返 "" → strategy_evidence 逐字节不变。第三层护栏。）

- [ ] **步骤 2：改 `PM_BATCH_SYSTEM`（prompts.py，紧随「历史反思」红线那条 `- `）**

```python
- 个股材料若含【盈余惊喜】：只用于收紧审慎——负向惊喜(miss)→更保守/止损复核，正向惊喜(beat)→减仓/止损前多确认防卖飞；禁止倒推成加仓/买入/更激进依据。
```

- [ ] **步骤 3：图编译 + 导入自检**

运行：
```bash
uv run python -c "from finance_agent.graph.workflow import build_graph; build_graph(); print('graph ok')"
```
预期：打印 `graph ok`（若入口函数名不同，先 `grep -n 'def build' src/finance_agent/graph/workflow.py` 定位）

- [ ] **步骤 4：跑相关回归确认注入未破原有拼接**

运行：`uv run pytest tests/test_db/test_reflection.py tests/ -k "reflection or pm_fallback or strategy" -q`
预期：PASS（原有 evidence/live/refl/dip/senti join 不变）

- [ ] **步骤 5：Commit**

```bash
git add src/finance_agent/graph/workflow.py src/finance_agent/agents/prompts.py
git commit -m "feat(P2b): strategy_node join sue_note + PM 红线·单向收紧（护栏第三层）"
```

---

## 任务 9：settings.yaml 双 flag 分层块

**文件：**
- 修改：`config/settings.yaml`（照 P1b 双 flag 注释块风格）

- [ ] **步骤 1：追加 flag 块（放 `sentiment_double_confirm_alert` 之后）**

```yaml
# ── P2b 盈余惊喜(SUE)因子 · 观测档（纯观测·默认 on）────────────────
# flag on：earnings-check 回看近3交易日财报→算 SUE→落 earnings_surprise_alerts 表；周六 value-report 回填30天漂移。
# 只读财报+写独立表，不碰任何 recommendation/仓位/权重。与 P1b/P1c/P2a 同治理级。
# off 则 earnings-check 逐字节不变、永不积累样本（无法验证覆盖度）——故默认 on 才能攒数据。
sue_factor:
  enabled: true
# ── P2b 盈余惊喜(SUE)因子 · 注入档（改核心建议·默认 off）──────────────
# flag on：日报 strategy_node 向 PM 注入双向审慎（miss→更保守·beat→减仓前多确认防卖飞），绝无加仓字样。
# 默认 off：format_sue_note 恒返 ""，strategy_evidence 逐字节与改动前一致（护栏第一层）。
# ⚠️ SUE 是占位因子，≥60条 outcome 经 RankIC 校准前禁当已验证 alpha、禁用于加仓。enable 须用户点头。
sue_pead_alert:
  enabled: false
  sigma_threshold: 1.5   # PRD 给定，直抄不做回测调参
  min_quarters: 8        # 历史 surprise 样本闸，不足则 SUE=None
  track_days: 30         # 漂移追踪/注入窗口（交易日约21，日历约30）
```

- [ ] **步骤 2：YAML 校验（hook 会自动跑，手动兜底）**

运行：`uv run python -c "import yaml; yaml.safe_load(open('config/settings.yaml')); print('yaml ok')"`
预期：打印 `yaml ok`

- [ ] **步骤 3：Commit**

```bash
git add config/settings.yaml
git commit -m "feat(P2b): settings 双 flag 分层（观测 on / 注入 off）"
```

---

## 任务 10：CLI 挂载（earnings-check 回看 + value-report 回填）

**文件：**
- 修改：`src/finance_agent/main.py`（两处 guarded 挂载）

- [ ] **步骤 1：定位挂载点**

运行：
```bash
grep -n "check_and_alert_earnings\|value.report\|_value_report\|earnings-check" src/finance_agent/main.py
```
记下 earnings-check 命令处理函数、value-report/sweep 处理函数的行号。

- [ ] **步骤 2：earnings-check 处理函数末尾追加（guarded）**

```python
    from finance_agent.signals.sue_factor import sue_factor_enabled
    if sue_factor_enabled():
        try:
            from finance_agent.alerts.earnings_trigger import record_sue_events
            rec = await record_sue_events()
            if rec:
                logger.info("SUE 观测：落库 %d 条盈余惊喜事件 %s", len(rec), rec)
        except Exception as e:
            logger.warning("SUE 回看落库失败（不拖垮 earnings-check）：%s", e)
```

（`logger` 用该文件现成 logger；若命令是同步函数则把 `await record_sue_events()` 包 `asyncio.run(...)`——按定位到的处理函数签名决定。）

- [ ] **步骤 3：value-report sweep 处理函数末尾追加（guarded，与影子A/B sweep 同款）**

```python
    from finance_agent.signals.sue_factor import sue_factor_enabled
    if sue_factor_enabled():
        try:
            from finance_agent.db.tracker import backfill_sue_outcomes
            res = await backfill_sue_outcomes()
            logger.info("SUE 漂移回填：%s", res)
        except Exception as e:
            logger.warning("SUE 回填失败（不拖垮 value-report）：%s", e)
```

- [ ] **步骤 4：CLI 冒烟（flag off 与 on 都不崩）**

运行：
```bash
uv run finance-agent earnings-check --skip-notify
```
预期：正常结束；观测档 on 时日志可见"SUE 观测：落库 N 条"或无事件时静默（无异常）。

- [ ] **步骤 5：Commit**

```bash
git add src/finance_agent/main.py
git commit -m "feat(P2b): earnings-check 挂 SUE 回看 + value-report 挂漂移回填（guarded）"
```

---

## 任务 11：flag-off 逐字节验证 + 全量回归 + 收口

**文件：** 无新增，验证 + 文档

- [ ] **步骤 1：全量测试零回归**

运行：`uv run pytest tests/ -x -q`
预期：全绿（原 493 + 本批新增 ~17）。若红先修再继续。

- [ ] **步骤 2：注入档 off 逐字节验证**

在 `config/settings.yaml` 确认 `sue_pead_alert.enabled: false`，跑日报干跑并确认 strategy_evidence 与改动前一致：
```bash
uv run finance-agent run --skip-notify 2>&1 | tail -40
```
预期：不崩；`format_sue_note` 恒返 "" → 无【盈余惊喜】字样出现在 PM 材料（可加临时断言或人工核对 diff）。

- [ ] **步骤 3：观测档 on 只写独立表验证**

确认 `sue_factor.enabled: true`，跑 earnings-check，检查只有 `earnings_surprise_alerts` 表被写、`recommendations`/仓位/权重路径未碰：
```bash
uv run finance-agent earnings-check --skip-notify
uv run python -c "from finance_agent.db.tracker import get_sue_alerts; print(len(get_sue_alerts()))"
```

- [ ] **步骤 4：更新 进展.md（现在段 + 素材台账 + 决策史）**

- 现在段：P2b 从"待实现"改为"✅ 实现落地 merge <sha>（flag off·注入档 enable 待点头）"，记 flag 现状 + 观察窗口 + "校准前禁对外宣传"声明。
- 决策史加一条：format_sue_note 走新鲜事件（非成熟事件）的澄清判定 + 未来函数为何不违反。
- 素材台账：SUE 设计规格那行状态可留"待出片"。

- [ ] **步骤 5：Commit + 收口**

```bash
git add 进展.md
git commit -m "docs(P2b): SUE 实现落地留痕·flag 现状·format 走新鲜事件澄清"
```

随后按项目「三处同步」惯例 ff 到 main + push（用户已授权推送模式；或按 /收一轮 流程发 demo 到飞书）。

---

## 自检结果（对照 spec）

**规格覆盖度：** §4.1 compute_sue/flags/format→任务1/6；§4.2 取数+解析→任务2/3；§4.3 事件表+回填→任务4/5；§4.4 earnings-check 回看→任务7；§4.5 strategy_node+PM→任务8；§4.6 flag→任务9；CLI 挂载→任务10；§8 测试全覆盖（11 纯函数 + 4 事件表，分布在任务1/2/4/5/6/7）；§10 验收→任务11。**§4.1"已成熟"与 §5 北极星的张力已在顶部澄清并判定（注入走新鲜事件）——待用户确认。**

**占位符扫描：** 无 TODO/待补；每步含可运行代码与命令。任务10 因缺 main.py 行号，含"grep 定位"步骤 + 完整插入代码（非占位）。

**类型一致性：** `save_sue_alert`/`get_sue_alerts`/`backfill_sue_outcomes`/`format_sue_note`/`record_sue_events`/`_extract_surprises`/`compute_sue` 签名跨任务一致；事件表列名（earnings_date/sue_score/return_30d/benchmark_return_30d）DDL→CRUD→回填→format 全程一致。

---

## 执行交接

**计划已保存到 `docs/superpowers/plans/2026-07-05-p2b-sue.md`。两种执行方式：**

1. **子代理驱动（推荐）** — 每任务一个新子代理 + 两阶段审查（superpowers:subagent-driven-development）
2. **内联执行** — 当前会话批量执行并设检查点（superpowers:executing-plans）

**执行前先确认顶部「设计澄清」：format_sue_note 走新鲜事件（预警）而非成熟事件（回顾）——这是本功能价值所在。**
